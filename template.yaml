AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  Event Registration & Ticketing System
  Serverless REST API built with AWS SAM (Lambda + API Gateway + DynamoDB)

# ---------------------------------------------------------------------------
# PARAMETERS
# ---------------------------------------------------------------------------
Parameters:
  Stage:
    Type: String
    Default: dev
    Description: Deployment stage (dev, staging, prod)
  NotificationEmail:
    Type: String
    Default: ""
    Description: >
      Optional email address to receive SNS confirmation emails and
      CloudWatch alarm notifications. Leave blank to skip email setup.

Conditions:
  HasNotificationEmail: !Not [!Equals [!Ref NotificationEmail, ""]]

# ---------------------------------------------------------------------------
# GLOBALS - shared settings applied to every function
# ---------------------------------------------------------------------------
Globals:
  Function:
    Runtime: python3.12
    Timeout: 10
    MemorySize: 128
    Tracing: Active
    Environment:
      Variables:
        EVENTS_TABLE: !Ref EventsTable
        REGISTRATIONS_TABLE: !Ref RegistrationsTable
        SNS_TOPIC_ARN: !If [HasNotificationEmail, !Ref ConfirmationTopic, ""]
  Api:
    Cors:
      AllowMethods: "'GET,POST,DELETE,OPTIONS'"
      AllowHeaders: "'Content-Type,Authorization'"
      AllowOrigin: "'*'"
    Auth:
      Authorizers:
        AdminAuthorizer:
          UserPoolArn: !GetAtt AdminUserPool.Arn

# ---------------------------------------------------------------------------
# RESOURCES
# ---------------------------------------------------------------------------
Resources:

  # ---------------------------------------------------------------------
  # DynamoDB Tables
  # ---------------------------------------------------------------------
  EventsTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub "events-${Stage}"
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: eventId
          AttributeType: S
      KeySchema:
        - AttributeName: eventId
          KeyType: HASH

  RegistrationsTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub "registrations-${Stage}"
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: registrationId
          AttributeType: S
        - AttributeName: email
          AttributeType: S
      KeySchema:
        - AttributeName: registrationId
          KeyType: HASH
      GlobalSecondaryIndexes:
        - IndexName: EmailIndex
          KeySchema:
            - AttributeName: email
              KeyType: HASH
          Projection:
            ProjectionType: ALL

  # ---------------------------------------------------------------------
  # Cognito - protects staff-only registration management routes
  # ---------------------------------------------------------------------
  AdminUserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: !Sub "home-in-prison-admins-${Stage}"
      AutoVerifiedAttributes: [email]
      UsernameAttributes: [email]
      Policies:
        PasswordPolicy:
          MinimumLength: 12
          RequireLowercase: true
          RequireNumbers: true
          RequireSymbols: true
          RequireUppercase: true

  AdminUserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      UserPoolId: !Ref AdminUserPool
      ClientName: !Sub "home-in-prison-admin-dashboard-${Stage}"
      GenerateSecret: false
      ExplicitAuthFlows:
        - ALLOW_REFRESH_TOKEN_AUTH
        - ALLOW_USER_PASSWORD_AUTH

  AdminGroup:
    Type: AWS::Cognito::UserPoolGroup
    Properties:
      GroupName: Admins
      UserPoolId: !Ref AdminUserPool
      Description: Foundation staff authorised to manage registrations

  # ---------------------------------------------------------------------
  # Optional SNS Topic for confirmation emails + alarm notifications
  # ---------------------------------------------------------------------
  ConfirmationTopic:
    Type: AWS::SNS::Topic
    Condition: HasNotificationEmail
    Properties:
      TopicName: !Sub "event-registration-notifications-${Stage}"

  ConfirmationSubscription:
    Type: AWS::SNS::Subscription
    Condition: HasNotificationEmail
    Properties:
      TopicArn: !Ref ConfirmationTopic
      Protocol: email
      Endpoint: !Ref NotificationEmail

  # ---------------------------------------------------------------------
  # Lambda Functions (one per endpoint)
  # ---------------------------------------------------------------------
  RegisterFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "event-register-${Stage}"
      CodeUri: src/handlers/
      Handler: register.handler
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref RegistrationsTable
        - DynamoDBReadPolicy:
            TableName: !Ref EventsTable
        - Statement:
            - Effect: Allow
              Action: sns:Publish
              Resource: !If [HasNotificationEmail, !Ref ConfirmationTopic, !Sub "arn:aws:sns:${AWS::Region}:${AWS::AccountId}:no-topic"]
      Events:
        RegisterApi:
          Type: Api
          Properties:
            Path: /register
            Method: post

  ListEventsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "event-list-${Stage}"
      CodeUri: src/handlers/
      Handler: list_events.handler
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref EventsTable
      Events:
        ListEventsApi:
          Type: Api
          Properties:
            Path: /events
            Method: get

  GetRegistrationsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "event-get-registrations-${Stage}"
      CodeUri: src/handlers/
      Handler: get_registrations.handler
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref RegistrationsTable
      Events:
        GetRegistrationsApi:
          Type: Api
          Properties:
            Path: /registrations/{email}
            Method: get

  CancelRegistrationFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "event-cancel-registration-${Stage}"
      CodeUri: src/handlers/
      Handler: cancel_registration.handler
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref RegistrationsTable
      Events:
        CancelRegistrationApi:
          Type: Api
          Properties:
            Path: /registration/{id}
            Method: delete

  AdminListRegistrationsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "event-admin-list-registrations-${Stage}"
      CodeUri: src/handlers/
      Handler: admin_list_registrations.handler
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref RegistrationsTable
      Events:
        AdminListRegistrationsApi:
          Type: Api
          Properties:
            Path: /admin/registrations
            Method: get
            Auth:
              Authorizer: AdminAuthorizer

  AdminCancelRegistrationFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "event-admin-cancel-registration-${Stage}"
      CodeUri: src/handlers/
      Handler: admin_cancel_registration.handler
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref RegistrationsTable
      Events:
        AdminCancelRegistrationApi:
          Type: Api
          Properties:
            Path: /admin/registration/{id}
            Method: delete
            Auth:
              Authorizer: AdminAuthorizer

  # ---------------------------------------------------------------------
  # CloudWatch Alarms - error rate > 5% per function
  # (Metric math: Errors / Invocations * 100)
  # ---------------------------------------------------------------------
  RegisterErrorRateAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "RegisterFunction-ErrorRate-${Stage}"
      AlarmDescription: "Triggers when RegisterFunction error rate exceeds 5%"
      EvaluationPeriods: 1
      DatapointsToAlarm: 1
      Threshold: 5
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: notBreaching
      AlarmActions:
        - !If [HasNotificationEmail, !Ref ConfirmationTopic, !Ref "AWS::NoValue"]
      Metrics:
        - Id: errorRate
          Expression: "(errors / invocations) * 100"
          Label: ErrorRatePercent
        - Id: errors
          MetricStat:
            Metric:
              Namespace: AWS/Lambda
              MetricName: Errors
              Dimensions:
                - Name: FunctionName
                  Value: !Ref RegisterFunction
            Period: 300
            Stat: Sum
          ReturnData: false
        - Id: invocations
          MetricStat:
            Metric:
              Namespace: AWS/Lambda
              MetricName: Invocations
              Dimensions:
                - Name: FunctionName
                  Value: !Ref RegisterFunction
            Period: 300
            Stat: Sum
          ReturnData: false

  AllFunctionsLogGroupRetention:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/aws/lambda/event-register-${Stage}"
      RetentionInDays: 14

# ---------------------------------------------------------------------------
# OUTPUTS
# ---------------------------------------------------------------------------
Outputs:
  ApiUrl:
    Description: Base URL for the implicit API Gateway Prod stage
    Value: !Sub "https://${ServerlessRestApi}.execute-api.${AWS::Region}.amazonaws.com/Prod"
  EventsTableName:
    Value: !Ref EventsTable
  RegistrationsTableName:
    Value: !Ref RegistrationsTable
  AdminUserPoolId:
    Description: Paste into the admin dashboard Cognito settings
    Value: !Ref AdminUserPool
  AdminUserPoolClientId:
    Description: Paste into the admin dashboard Cognito settings
    Value: !Ref AdminUserPoolClient
